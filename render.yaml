services:
  - type: web
    name: servicenow-mcp
    runtime: python
    buildCommand: pip install -r requirements.txt
    startCommand: python server.py
    envVars:
      - key: SNOW_INSTANCE_URL
        sync: false
      - key: SNOW_USERNAME
        sync: false
      - key: SNOW_PASSWORD
        sync: false
